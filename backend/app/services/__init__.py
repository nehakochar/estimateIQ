# This file makes the 'services' directory a Python package.
#
# What goes here?
# Business logic — the code that does the actual work.
# Routes call services; services call models/external APIs.
# This separation keeps routes thin and logic testable.
#
# Example (future):
#   from app.services.rfp_service import parse_rfp, create_rfp
