# ── API Framework ─────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# ── Settings ──────────────────────────────────────────────────────
pydantic-settings==2.9.1

# ── Database ──────────────────────────────────────────────────────
sqlalchemy==2.0.41
psycopg2-binary==2.9.10

# ── Task Queue ────────────────────────────────────────────────────
celery[redis]==5.4.0
redis==5.2.1

# ── Document Parsing ──────────────────────────────────────────────
# pdfplumber: table-aware PDF extraction for the LLM extraction pipeline
pdfplumber==0.11.4

# PyMuPDF: used by existing parsers/pdf_parser.py (kept for parsing step)
PyMuPDF==1.25.5

# python-docx: Word document parsing
python-docx==1.1.2

# openpyxl: Excel parsing
openpyxl==3.1.5

# ── LLM Providers (pick one, set EXTRACTION_PROVIDER in .env) ────
# Gemini 2.0 Flash — FREE, 1500 req/day, 1M token context (RECOMMENDED)
# Get key: aistudio.google.com
google-generativeai==0.8.3

# Groq llama-3.3-70b — FREE, 14400 req/day, 128K context
# Get key: console.groq.com
groq==0.11.0

# Pin httpx to 0.27.2 to avoid "Client.__init__() got an unexpected keyword argument 'proxies'" error in SDK clients
httpx==0.27.2
